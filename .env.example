# ── Database ─────────────────────────────────────────────────────────────────
# Neon PostgreSQL connection string (asyncpg driver)
DATABASE_URL=postgresql+asyncpg://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require

# ── Application ───────────────────────────────────────────────────────────────
APP_URL=https://trustpages.fly.dev
JWT_SECRET=change-me-to-a-long-random-secret-at-least-32-chars

# ── LLM ───────────────────────────────────────────────────────────────────────
GEMINI_API_KEY=AIza...

# ── Email (Resend) ────────────────────────────────────────────────────────────
RESEND_API_KEY=re_live_...
# MUST be an address on a domain verified in Resend. onboarding@resend.dev is a
# shared sandbox sender that only ever delivers to the account owner's own
# address — subscriber notifications to anyone else are silently dropped.
RESEND_FROM_EMAIL=TrustPages <noreply@usetrustpages.com>

# ── Billing (Paddle) ───────────────────────────────────────────────────────────
# Server-side API key (Paddle > Developer tools > Authentication) — keep secret.
PADDLE_API_KEY=pdl_...
# Client-side token (Paddle > Developer tools > Authentication) — safe to expose
# in frontend code, used by Paddle.js on the checkout page.
PADDLE_CLIENT_TOKEN=live_...
# Webhook signing secret (Paddle > Developer tools > Notifications > your endpoint).
PADDLE_WEBHOOK_SECRET=pdl_ntfset_...
PADDLE_PRICE_ID_GROWTH=pri_...
PADDLE_PRICE_ID_GROWTH_YEARLY=pri_...
# Starter is the middle tier. Leave blank until a real price exists in
# Paddle for it — checkout returns 503 for a blank price id, same as it
# already does for Growth, rather than starting a checkout with no price.
PADDLE_PRICE_ID_STARTER=
PADDLE_PRICE_ID_STARTER_YEARLY=
# "sandbox" while testing, "production" once live.
PADDLE_ENVIRONMENT=sandbox

# ── RFC 3161 timestamping (optional — defaults shown) ─────────────────────────
# Only FreeTSA is configured for now — leave TSA_FALLBACK_URL blank until a
# second provider's CA chain is sourced and bundled in
# app/static_data/tsa/. Setting it without that chain makes a
# fallback-issued token unverifiable by verify.sh/ /verify.
# TSA_PRIMARY_URL=https://freetsa.org/tsr
# TSA_FALLBACK_URL=
# TSA_TIMEOUT_SECONDS=20
# TSA_MAX_ATTEMPTS=5

# ── Monitoring reliability (optional — defaults shown) ───────────────────────
# Consecutive failed checks before a source gets the "Monitoring Alert" badge
# and email.
# MONITORING_ALERT_FAILURE_THRESHOLD=3
# Days to wait before re-sending an alert email for the same ongoing issue —
# shared by both the failure-count alarm and the staleness alarm below.
# MONITORING_ALERT_DEDUPE_DAYS=7
# Independent second alarm: a source not successfully checked in this many
# days alerts regardless of *why* (failure streak, budget deferral, anything).
# STALENESS_ALERT_DAYS=3
# Per-SOURCE, per-UTC-day Tier-2 (Playwright) run cap — the real cost
# control. See the sweep-cost note in CLAUDE.md before raising it.
# TIER2_DAILY_PER_SOURCE=2
# Per-tenant, per-UTC-day Tier-2 pool — a cost SAFETY VALVE, not a feature
# limit (every plan gets full Tier-2 escalation); sized well above what
# per-source quotas should ever add up to.
# TIER2_DAILY_LIMIT_FREE=6
# TIER2_DAILY_LIMIT_STARTER=20
# TIER2_DAILY_LIMIT_GROWTH=60
# Below this many characters of normalized visible text, a fetched page is
# treated as a failed check (bot wall / empty / script-only) rather than
# real content.
# CONTENT_HEALTH_MIN_TEXT_LENGTH=500

# ── Observability (optional) ──────────────────────────────────────────────────
# SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz

# ── Analytics (optional) ───────────────────────────────────────────────────────
# GA4 measurement ID from analytics.google.com → Admin → Data Streams
# GA_MEASUREMENT_ID=G-XXXXXXX
